# .envrc

# Set the C++ include path
set -x CPLUS_INCLUDE_PATH "$PWD/include:$CPLUS_INCLUDE_PATH"

# Set compiler flags
set -x CXXFLAGS "-I$PWD/include -std=c++17"

# Optionally, if using clangd with compile_commands.json:
set -x COMPILE_COMMANDS_DIR "$PWD/build"
