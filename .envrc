# .envrc

# Set the C++ include path
export CPLUS_INCLUDE_PATH="$PWD/include:$CPLUS_INCLUDE_PATH"

# Set compiler flags
export CXXFLAGS="-I$PWD/include -std=c++17"

# Optionally, if using clangd with compile_commands.json:
export COMPILE_COMMANDS_DIR="$PWD/build"
